frr:
  # The name of the package
  package: frr
  package_auto_upgrade: False
  # Used to fetch FRR directly from the releases page on GitHub.
  package_url: False
  # Hash to proove the integrity of the package file. (Only used with package_url.)
  package_hash: False
  # Custom repo RPM url for CentOS.  https://rpm.frrouting.org/
  custom_repo_rpm: False
  # Custom repo name.  Valid names: frr-6-repo, frr-7-repo or frr-stable-repo
  custom_repo_name: False
  service: frr
  conf_dir: /etc/frr
  log_dir: /var/log/frr
  run_dir: /var/run/frr
  user: frr
  group: frr
  vtygroup: frrvty
  use_daemons_file: True
  manage_sysrc: False
  use_integrated_mode: True
  one_service_to_start_them_all: True
  use_vtysh: True
